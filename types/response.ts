import type { TUsers } from '~/types/users/users';
import type { TBrand } from './brands';
export interface TResponse {
    data: any;
    status: number;
    statusCode: number;
    message: string;
}